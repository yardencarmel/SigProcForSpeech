# F5-TTS Acoustic Style Transfer Extension
# Requirements file
#
# Base F5-TTS dependencies (will be installed via pip install -e F5-TTS)

# Core F5-TTS dependencies (these will be installed when cloning F5-TTS)
torch>=2.0.0
torchaudio>=2.0.0
transformers>=4.36.0
accelerate>=0.33.0
einops>=0.8.0
vocos
pydub
soundfile
cached_path
jieba
pypinyin
safetensors
hydra-core>=1.3.0
omegaconf
wandb

# Audio processing
librosa>=0.10.0
scipy>=1.10.0
numpy>=1.24.0

# Data handling
pandas>=2.0.0
tqdm>=4.65.0
requests>=2.28.0              # HTTP client for API calls
python-dotenv>=1.0.0          # Load environment variables from .env

# Gradio for optional web interface
gradio>=4.0.0

# Evaluation
openai-whisper               # WER via Whisper large-v3 (~3 GB download on first run)
matplotlib>=3.7.0            # Result plots

# Development tools (optional)
tensorboard>=2.14.0
